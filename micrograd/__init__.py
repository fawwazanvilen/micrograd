# turns out you need an __init__.py file (even if empty) so that the python interpreter recognizes 
# this as a package

# if we don't have this, we can't import files within the micrograd directory
# as part of the package

# can be filled in if some initialization code is needed
